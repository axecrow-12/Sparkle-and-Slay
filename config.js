// Keep false while working with the local frontend. Set true when the API is ready.
const USE_BACKEND = false;

// Change this to your deployed backend URL when going live.
const API_BASE = "http://localhost:4000/api";
